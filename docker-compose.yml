version: '3.8'

services:
  code-generator:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GEMINI_API_KEY=votre_cle_api
    volumes:
      - ./data:/app/data
      - ./templates:/app/templates