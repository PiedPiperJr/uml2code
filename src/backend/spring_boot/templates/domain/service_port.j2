package {{ ir.package }}.domain.ports.driving;

import {{ ir.package }}.domain.entities.{{ entity.name }};
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import java.util.UUID;

public interface I{{ entity.name }}Service {
    {{ entity.name }} create({{ entity.name }} entity);
    {{ entity.name }} findById(UUID id);
    Page<{{ entity.name }}> findAll(Pageable pageable);
    {{ entity.name }} update(UUID id, {{ entity.name }} entity);
    void delete(UUID id);
}
