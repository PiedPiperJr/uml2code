package {{ ir.package }}.application.ports.crud.{{ entity.name | lower }};

import {{ ir.package }}.application.dto.Create{{ entity.name }}Request;
import {{ ir.package }}.application.dto.{{ entity.name }}Response;

/** Entry point into the application — use-case contract for creating a {{ entity.name }}. */
public interface ICreate{{ entity.name }} {
    {{ entity.name }}Response execute(Create{{ entity.name }}Request request);
}
