package {{ ir.package }}.application.ports.crud.{{ entity.name | lower }};

import java.util.UUID;

/** Entry point into the application — use-case contract for deleting a {{ entity.name }}. */
public interface IDelete{{ entity.name }} {
    void execute(UUID id);
}
