package {{ ir.package }}.application.ports.crud.{{ entity.name | lower }};

import {{ ir.package }}.application.dto.{{ entity.name }}SummaryResponse;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

/** Entry point into the application — use-case contract for paginated listing of {{ entity.name }}. */
public interface IFindAll{{ entity.name }} {
    Page<{{ entity.name }}SummaryResponse> execute(Pageable pageable);
}
