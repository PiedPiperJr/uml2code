package {{ ir.package }}.application.ports.crud.{{ entity.name | lower }};

import {{ ir.package }}.application.dto.{{ entity.name }}Response;
import java.util.UUID;

/** Entry point into the application — use-case contract for fetching a {{ entity.name }} by id. */
public interface IFindById{{ entity.name }} {
    {{ entity.name }}Response execute(UUID id);
}
