package {{ ir.package }}.application.usecases;

import {{ ir.package }}.application.dto.{{ entity.name }}DtoMapper;
import {{ ir.package }}.application.dto.{{ entity.name }}Response;
import {{ ir.package }}.application.ports.IFindById{{ entity.name }};
import {{ ir.package }}.domain.entities.{{ entity.name }};
import {{ ir.package }}.domain.exceptions.EntityNotFoundException;
import {{ ir.package }}.domain.ports.I{{ entity.name }}Repository;
import java.util.UUID;

public class FindById{{ entity.name }}UseCase implements IFindById{{ entity.name }} {

    private final I{{ entity.name }}Repository repository;
    private final {{ entity.name }}DtoMapper mapper;

    public FindById{{ entity.name }}UseCase(I{{ entity.name }}Repository repository, {{ entity.name }}DtoMapper mapper) {
        this.repository = repository;
        this.mapper = mapper;
    }

    @Override
    public {{ entity.name }}Response execute(UUID id) {
        {{ entity.name }} entity = findOrThrow(id);
        return toResponse(entity);
    }

    private {{ entity.name }} findOrThrow(UUID id) {
        return repository.findById(id)
                .orElseThrow(() -> new EntityNotFoundException("{{ entity.name }}", id));
    }

    private {{ entity.name }}Response toResponse({{ entity.name }} entity) {
        return mapper.toResponse(entity);
    }
}
