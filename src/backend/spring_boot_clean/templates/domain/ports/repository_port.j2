package {{ ir.package }}.domain.ports;

import {{ ir.package }}.domain.entities.{{ entity.name }};
import java.util.List;
import java.util.Optional;
import java.util.UUID;

/**
 * Driven port — repository contract defined by the domain, implemented in infrastructure.
 */
public interface I{{ entity.name }}Repository {

    {{ entity.name }} save({{ entity.name }} entity);

    Optional<{{ entity.name }}> findById(UUID id);

    List<{{ entity.name }}> findAll(int page, int size);

    long count();

    boolean existsById(UUID id);

    void deleteById(UUID id);
}
